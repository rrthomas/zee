// Table of variables

/*
 * The first column specifies the variable name.
 * The second column specifies the default value.
 * The third column gives the variable's documentation.
 */

X("tab_display_width", "8", "Number of spaces displayed for a tab character.")
X("indent_width", "2", "Number per indentation for `edit_insert_tab'.")
X("wrap_mode", "false", "Whether wrap mode is automatically enabled.")
X("wrap_column", "72", "Column beyond which wrapping occurs in Wrap Mode.")
X("caseless_search", "true", "Whether searching ignores case by default.")
X("case_replace", "true", "Whether `edit_replace' should preserve case.")
